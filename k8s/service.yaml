apiVersion: v1
kind: Service
metadata:
  name: spring-ai-banking-assistant
spec:
  type: ClusterIP
  selector:
    app: spring-ai-banking-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8080
